# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MongoDB)
# MongoDB Atlas (Cloud) Connection
# Your MongoDB Atlas connection string with credentials
MONGODB_URI=mongodb+srv://hamzaberahmaofficial_db_user:4H5d4pvO5PdwJoYZ@cluster0.dh2f0je.mongodb.net/sou9na?retryWrites=true&w=majority&appName=Cluster0

# Option 2: Local MongoDB (for development)
# Uncomment and use if running MongoDB locally
# MONGODB_URI=mongodb://localhost:27017/sou9na

# Option 3: Use individual connection parameters (alternative, for local MongoDB)
# DB_HOST=localhost
# DB_PORT=27017
# DB_NAME=sou9na

# JWT Configuration
JWT_SECRET=dev-secret-key-change-in-production
JWT_REFRESH_SECRET=dev-refresh-secret-key

# CIH API Configuration
CIH_API_BASE_URL=https://sandbox.cih.ma/api
CIH_API_KEY=test-key
CIH_HOLDING_WALLET_ID=holding-wallet-test

# Twilio Configuration (optional for development)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
